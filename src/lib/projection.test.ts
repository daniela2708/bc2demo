import {describe,expect,it} from 'vitest';import {current} from '../data/season';import {project} from './projection';
const all=new Set(current.map(r=>r.sub));
describe('projection',()=>{it('matches acceptance scenario',()=>{const x=project(current,{remainingPctSold:.65,markdown1:.25,secondRemainingPctSold:1,markdown2:.75,included:all});expect(x.p1.pcs).toBeCloseTo(31097.95,1);expect(x.p1.revenue).toBeCloseTo(104678,-0);expect(x.p2.pcs).toBeCloseTo(16745.05,1);expect(x.p2.revenue).toBeCloseTo(18788,-0);expect(x.totalRetail).toBeCloseTo(308180,-2);expect(x.p2.gp).toBeLessThan(-18000)});it('100% markdown can create negative GP',()=>expect(project(current,{remainingPctSold:1,markdown1:1,secondRemainingPctSold:0,markdown2:0,included:all}).p1.gp).toBeLessThan(0));it('zero remaining is stable',()=>{const r=[{sub:'x',purchPcs:1,purchCost:2,soldPcs:1,soldRetail:3,retailGp:1}];expect(project(r,{remainingPctSold:1,markdown1:0,secondRemainingPctSold:1,markdown2:0,included:new Set(['x'])}).projectedRetail).toBe(0)});it('all excluded returns sold-only totals',()=>{const x=project(current,{remainingPctSold:.5,markdown1:.5,secondRemainingPctSold:1,markdown2:.5,included:new Set()});expect(x.projectedRetail).toBe(0);expect(x.totalRetail).toBe(184714)})});
