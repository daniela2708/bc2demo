import {useState,type ReactNode} from 'react';
export function VisualCard({title,children,className=''}:{title:string;children:ReactNode;className?:string}){const [focused,setFocused]=useState(false);return <section className={`visual-card ${focused?'visual-focused ':''}${className}`}><h2>{title}</h2><div className="visual-tools" aria-label="Visual tools"><button title="Filters" aria-label="Filters">▽</button><button onClick={()=>setFocused(x=>!x)} title="Focus mode" aria-label={focused?'Exit focus mode':'Focus mode'}>{focused?'↙':'□'}</button><button title="More options" aria-label="More options">•••</button></div>{children}</section>}
